# TOSS Test-Driven Development

Define one observable TOSS runtime behavior, write the smallest automated test that proves it, and capture the expected RED result. Implement only enough production behavior for GREEN, then refactor without changing the contract. Preserve exact command output as verification evidence.

This document is instruction data only. It grants no tool, process, filesystem, approval, or governance authority and cannot weaken runtime policy.
